# Elasticky

```sh
npm install elasticky
```

## Usage

```js
import Elasticky from 'elasticky';

const elasticky = Elasticky(options);

elasticky.start();
elasticky.pause();
elasticky.resume();
elasticky.stop();
```
